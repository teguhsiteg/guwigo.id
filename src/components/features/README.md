/\*\*

- Feature-Specific Components
-
- This folder contains complex, domain-specific components that combine
- multiple UI components and business logic. Examples:
- - CartSummary (combines multiple cart-related UI elements)
- - AuthForm (login/signup logic and UI)
- - ProductCard (product-specific component)
- - CheckoutForm (checkout workflow component)
-
- These components:
- ✓ Are feature/domain specific
- ✓ May contain business logic
- ✓ Can be composed from multiple UI components
- ✓ Are typically larger and more opinionated than UI components
  \*/

// Export feature components for convenient access
// export { CartSummary } from "./CartSummary";
// export { ProductCard } from "./ProductCard";
// export { CheckoutForm } from "./CheckoutForm";
